import classes from "./NavBar.module.scss";

const NavBar = () => {
  return (
    <div className={classes.wrapper}>
      <nav className={classes.navbar}>меню</nav>
    </div>
  );
};

export default NavBar;
